<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Animated Signup Form</title>
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap"
    rel="stylesheet"
  />
  <script
    src="https://kit.fontawesome.com/a81368914c.js"
    crossorigin="anonymous"
    defer
  ></script>
  <style>
    /* Reset and basics */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #71b7e6, #9b59b6);
      padding: 20px;
      /* Removed full viewport height and flex centering */
    }

    /* Signup form container */
    .signup-form {
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
      width: 400px;
      padding: 40px 30px;
      color: #333;
      margin: 60px auto 0 auto; /* Center horizontally and add space from top */
    }

    /* Form header */
    .signup-form h2 {
      text-align: center;
      margin-bottom: 30px;
      font-weight: 600;
      color: #5a2a82;
      letter-spacing: 1px;
    }

    /* Input container */
    .input-group {
      position: relative;
      margin-bottom: 25px;
    }

    /* Icon on left */
    .input-group .icon {
      position: absolute;
      top: 50%;
      left: 12px;
      transform: translateY(-50%);
      color: #999;
      font-size: 18px;
      pointer-events: none;
    }

    /* Input fields */
    .input-group input,
    .input-group select {
      width: 100%;
      padding: 14px 14px 14px 40px;
      font-size: 16px;
      border: 2px solid #ddd;
      border-radius: 8px;
      outline: none;
      transition: border-color 0.3s;
      background: transparent;
      color: #444;
    }

    .input-group input:focus,
    .input-group select:focus {
      border-color: #5a2a82;
      box-shadow: 0 0 8px rgba(90, 42, 130, 0.4);
    }

    /* Floating label */
    .input-group label {
      position: absolute;
      top: 50%;
      left: 40px;
      color: #aaa;
      font-size: 16px;
      pointer-events: none;
      transform: translateY(-50%);
      transition: 0.3s ease all;
      background: white;
      padding: 0 6px;
    }

    /* When input is focused or filled */
    .input-group input:focus + label,
    .input-group input:not(:placeholder-shown) + label,
    .input-group select:focus + label,
    .input-group select:not([value=""]) + label {
      top: -8px;
      font-size: 13px;
      color: #5a2a82;
      font-weight: 600;
    }

    /* Submit button */
    .btn-submit {
      width: 100%;
      padding: 14px;
      border: none;
      border-radius: 8px;
      background: #5a2a82;
      color: white;
      font-size: 18px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.3s ease;
      letter-spacing: 1px;
    }

    .btn-submit:hover {
      background: #7b42b8;
    }

    /* Link below */
    .form-footer {
      margin-top: 18px;
      text-align: center;
      font-size: 14px;
      color: #555;
    }

    .form-footer a {
      color: #5a2a82;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s;
    }

    .form-footer a:hover {
      color: #7b42b8;
    }

    /* Responsive */
    @media (max-width: 440px) {
      .signup-form {
        width: 100%;
        padding: 30px 20px;
      }
    }
  </style>
</head>
<body>
  <!-- Your existing header would be here -->
  <!-- Example header placeholder -->
  
  </header>

  <form class="signup-form" action="/signup" method="POST" novalidate>
    <h2>Create Account</h2>

    <div class="input-group">
      <i class="fas fa-user icon"></i>
      <input
        type="text"
        name="firstName"
        id="firstName"
        required
        autocomplete="given-name"
        placeholder=" "
      />
      <label for="firstName">First Name</label>
    </div>

    <div class="input-group">
      <i class="fas fa-user icon"></i>
      <input
        type="text"
        name="lastName"
        id="lastName"
        required
        autocomplete="family-name"
        placeholder=" "
      />
      <label for="lastName">Last Name</label>
    </div>

    <div class="input-group">
      <i class="fas fa-envelope icon"></i>
      <input
        type="email"
        name="email"
        id="email"
        required
        autocomplete="email"
        placeholder=" "
      />
      <label for="email">Email Address</label>
    </div>

    <div class="input-group">
      <i class="fas fa-lock icon"></i>
      <input
        type="password"
        name="password1"
        id="password1"
        required
        autocomplete="new-password"
        placeholder=" "
      />
      <label for="password1">Password</label>
    </div>

    <div class="input-group">
      <i class="fas fa-lock icon"></i>
      <input
        type="password"
        name="password2"
        id="password2"
        required
        autocomplete="new-password"
        placeholder=" "
      />
      <label for="password2">Confirm Password</label>
    </div>

    <div class="input-group">
      <i class="fas fa-user-tag icon"></i>
      <select name="role" id="role" required>
        <option value="" disabled selected hidden></option>
        <option value="donor">Donor</option>
        <option value="agent">Agent</option>
      </select>
      <label for="role">Signup As</label>
    </div>

    <button class="btn-submit" type="submit">Sign Up</button>

    <div class="form-footer">
      Already have an account? <a href="/auth/login">Login here</a>
    </div>
  </form>
</body>
</html>